# HedgeAI Demo 演示脚本

## 目标

在 3 分钟内演示以下能力：

- 加载高风险示例仓位
- 自动触发风险扫描
- AI 生成 3 套对冲方案
- 点击执行并返回 Injective Demo 交易哈希

## 演示前准备

1. 启动后端：`uvicorn main:app --reload --port 8000`
2. 启动前端：`npm run dev`
3. 准备至少一个可用模型 API Key，推荐 Claude
4. 打开 `http://localhost:5173`

## 推荐演示流程

### 0:00 - 0:20

- 打开页面
- 进入“设置”
- 选择 Claude 并填入 API Key

讲解点：

- 这是一个面对普通合约交易者的 AI 风控助手
- 不要求用户会看 Greeks，也不要求理解期权结构

### 0:20 - 0:40

- 在 Injective 卡片点击“加载示例仓位”

讲解点：

- Demo 内置了一组高风险 BTC 多单
- 这样即使没有真实账户，也能完整演示系统闭环

### 0:40 - 1:10

- 等待风险扫描触发
- 页面顶部出现高风险告警横幅
- 聊天区自动发起风险分析请求

讲解点：

- 系统不是被动问答，而是会主动发现风险
- 风险扫描会读取账户仓位并识别接近爆仓的持仓

### 1:10 - 1:50

- AI 输出中文分析
- 底部出现 3 套策略卡片

讲解点：

- 方案 A：反向合约，适合最快速降风险
- 方案 B：Polymarket，适合低频事件型对冲思路
- 方案 C：Injective 期权，适合保底风险但成本更高

### 1:50 - 2:20

- 点击任意策略卡的“执行此方案”
- 等待返回交易哈希

讲解点：

- 后端会基于当前仓位方向、仓位名义价值和策略对冲比例推导执行参数
- 没有配置真实私钥时，会走 Demo 执行模式，但仍会给出完整哈希格式

### 2:20 - 3:00

- 展示执行结果
- 如有 Explorer 链接可打开

结束话术：

- 这证明 HedgeAI 不只是聊天机器人，而是从风险识别、策略生成到执行回执的完整 Agent Demo

## 推荐提问文案

- `分析我当前仓位的爆仓风险`
- `我的 BTC 多单已经浮亏，给我三套可执行的对冲方案`
- `优先保护下行风险，控制成本，给我一个最稳妥的方案`

## 兜底方案

如果模型网络暂时不可用：

- 先展示示例仓位和风险横幅
- 再说明执行链路与参数推导逻辑
- 最后演示 Demo 执行返回的交易哈希

如果外部市场接口波动：

- 使用内置 `demo` 仓位
- 避免依赖现场输入真实地址

## 演示成功标准

- 能看到 Injective 示例仓位
- 能出现至少一条高风险告警
- AI 能生成结构化策略卡片
- 至少一张策略卡可以成功执行并返回 `tx_hash`
