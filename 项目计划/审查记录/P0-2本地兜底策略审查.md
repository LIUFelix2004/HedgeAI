# P0-2 本地兜底策略审查

## 范围

- 分支：`feat/d0-tdd-collaboration-infra`
- 阶段：P0-2 AI 失败兜底策略卡片
- 日期：2026-05-29
- 监督者：等待返回

## 交付内容

- 新增 `frontend/src/lib/fallbackStrategies.js`。
- 新增本地兜底三策略：
  - A：`REVERSE_HEDGE`
  - B：`POLYMARKET`
  - C：`OPTIONS`
- `sendChatMessage()` 在两种场景进入兜底：
  - `sendMessageStream` 抛错。
  - 流式成功但未解析到结构化策略。
- 兜底策略包含：
  - `source="fallback"`
  - `confidence="local-rule"`
  - `fallback_reason`
- 无仓位时不生成策略，只提示先加载 Demo 仓位。
- `ChatMessage` 已显示“本地兜底”标签，`HedgeCard` 已对 `source="fallback"` 显示兜底标签。

## TDD 证据

### Red

- 前端 Red 命令：`fnm.exe exec --using v24.16.0 pnpm.cmd run test -- fallbackStrategies.test.js chat.test.js ChatMessage.test.jsx`
- Red 结果：
  - `fallbackStrategies.test.js` 失败于无法解析 `./fallbackStrategies`。
  - `chat.test.js` 失败于 assistant 缺少 `fallback_reason` / `strategies`。

### Green

- 前端窄测试：7 个测试文件、11 个测试通过。
- 前端完整测试：7 个测试文件、12 个测试通过。
- 前端构建：通过，1792 modules transformed。
- 后端完整测试：8 个测试通过。
- 后端语法检查：通过。
- 扩大乱码扫描：无命中。

## 风险审查

- [x] fallback 不伪装成 AI 输出。
- [x] fallback 标注 `source="fallback"`。
- [x] fallback 顶层结果也标注 `source="fallback"`、`confidence="local-rule"`。
- [x] fallback UI 显示“本地兜底”。
- [x] 无仓位时不凭空生成可执行策略。
- [x] 无仓位 chat fallback 已有回归测试，断言策略列表为空。
- [x] Polymarket / Options 兜底文案为参考或 dry-run 语义，不声称真实成交。

## Browser 可视 QA

- 结果：未完成。
- 原因：Browser 插件仍因 `windows sandbox failed: spawn setup refresh` 失败。
- 需人工补验：
  - 不填模型 API Key，发送策略请求后出现三张策略卡。
  - 策略卡显示“本地兜底”。
  - 无仓位时提示先加载 Demo 仓位。

## 当前判定

- 自动化验收：Go。
- Browser 工具验收：Blocked by tool。
- 监督者复审：等待返回。
- 监督者复审：Go。
- 复审建议处理：
  - `noMojibake.test.js` 扫描范围扩到 `backend/tests`。
  - fallback 顶层结果补充 `source` / `confidence` 元数据。
- 阶段结论：Go，允许进入 P0-4 / P1-1；Browser 可视 QA 待工具恢复后补验。
