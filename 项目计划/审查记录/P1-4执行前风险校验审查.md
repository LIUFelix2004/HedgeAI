# P1-4 执行前风险校验审查

## 范围

- 分支：`feat/d0-tdd-collaboration-infra`
- 阶段：P1-4 执行前风险校验
- 日期：2026-05-29
- 监督者：Lovelace（只读审查）

## 交付内容

- `ExecuteRequest` 增加 `idempotency_key`。
- 后端真实执行前增加 idempotency key 去重。
- 后端真实执行前增加名义金额上限 `MAX_REAL_ORDER_NOTIONAL = 100000`。
- 前端 `HedgeCard` 在 real 模式执行时生成并发送 idempotency key。
- 保持 demo / dry-run 路径不受影响。

## TDD 证据

### Red

- 后端 Red：`backend\.venv\Scripts\python.exe -m unittest backend.tests.test_execution_precheck`
- Red 结果：相同 `idempotency_key` 会重复调用真实服务；超额名义金额未在服务调用前阻断。
- 前端 Red：`fnm.exe exec --using v24.16.0 pnpm.cmd run test -- HedgeCard.execute.test.jsx`
- Red 结果：real confirmed payload 缺少 `idempotency_key`。

### Green

- 后端窄测：`backend\.venv\Scripts\python.exe -m unittest backend.tests.test_execution_precheck backend.tests.test_execute_modes`，8 tests 通过。
- 后端窄测（No-Go 修复后）：`backend\.venv\Scripts\python.exe -m unittest backend.tests.test_execution_precheck backend.tests.test_execute_modes`，9 tests 通过。
- 后端完整测试：`backend\.venv\Scripts\python.exe -m unittest discover backend\tests`，22 tests 通过。
- 后端语法检查：`backend\.venv\Scripts\python.exe -m py_compile backend\models\schemas.py backend\routers\hedge.py` 通过。
- 前端完整测试：`fnm.exe exec --using v24.16.0 pnpm.cmd run test`，15 files / 28 tests 通过。
- 前端构建：`fnm.exe exec --using v24.16.0 pnpm.cmd run build` 通过，1796 modules transformed。
- 乱码扫描：`frontend/src backend/routers backend/tests backend/models backend/services` 无命中。

## 安全审查

- [x] 重复真实执行请求会被 idempotency key 阻断。
- [x] real API 缺少 idempotency key 时会被后端阻断。
- [x] 超过真实订单名义金额上限时，不会调用真实下单服务。
- [x] demo / dry-run 不要求 idempotency key，仍走预览语义。
- [x] 执行失败不会显示成功。
- [ ] 余额和保证金校验仍未完成，后续归入 P2-1 / P1-4 后半段。
- [ ] 仓位刷新与仓位变化后二次确认仍未完成。

## Browser 可视 QA

- 结果：未完成。
- 原因：`node_repl` / Browser 自动化仍失败，错误为 `windows sandbox failed: spawn setup refresh`。
- 需要人工补验：
  - real 执行确认后按钮进入 loading，不允许重复点击。
  - 后端 blocked 错误能在卡片内显示，不被误认为成功。

## 当前判定

- 自动化验收：Go。
- Browser 工具验收：Blocked by tool。
- 监督者审查：No-Go 后修复。
- 监督者发现项与处理：
  - P1：后端 real API 允许不带 `idempotency_key`，重复提交保护可被绕过。已改为后端强制要求 key，并补 `test_real_execution_requires_idempotency_key`。
  - P2：idempotency key 为进程内 set，重启/多实例下无效。记录为后续 P2-4 / 生产化风险。
  - P2：失败请求占用 key 的重试语义需要明确。当前保持保守阻断，后续可改为持久化状态机。
- 阶段结论：Go；Browser 可视 QA 待工具恢复或人工补验。
