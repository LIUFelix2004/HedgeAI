# P0-3 文案乱码与执行态审查

## 范围

- 分支：`feat/d0-tdd-collaboration-infra`
- 阶段：P0-3 文案乱码修复、文案集中化、执行态安全提示
- 日期：2026-05-29
- 监督者：
  - 初审：`019e6fc9-5517-7992-9c6c-70f60c161d56`，结论 No-Go
  - 复审：`019e6fd1-e6c4-7fc0-b106-feadc83c710c`，结论 Go

## 初审 No-Go 处理

- [x] 前端用户可见文案继续收敛到 `frontend/src/lib/copy.js`。
- [x] `frontend/src/components` 与 `frontend/src/App.jsx` 已无散落中文文案。
- [x] 新增 `frontend/src/lib/executionStatus.js`，按 `execution_mode` 区分真实提交与模拟执行。
- [x] `backend/models/schemas.py` 的 `ExecuteResult` 新增 `execution_mode` 字段。
- [x] Polymarket mock/demo 成功结果返回 `execution_mode="demo"`，摘要显式包含“模拟执行”。
- [x] 前端 demo/dry-run 不再展示“交易已提交”。
- [x] 新增 TDD 证据测试：`frontend/src/lib/executionStatus.test.js`、`backend/tests/test_hedge_execute_mode.py`。

## TDD 证据

### Red

- 前端 Red 命令：`fnm.exe exec --using v24.16.0 pnpm.cmd run test`
- 前端 Red 结果：
  - `executionStatus.test.js` 失败于无法解析 `./executionStatus`。
  - `copy.test.js` 失败于 `COPY.chatInput` 缺失。
- 后端 Red 命令：`backend\.venv\Scripts\python.exe -m unittest backend.tests.test_hedge_execute_mode`
- 后端 Red 结果：
  - `KeyError: 'execution_mode'`。

### Green

- 前端测试命令：`fnm.exe exec --using v24.16.0 pnpm.cmd run test`
- 前端测试结果：3 个测试文件、5 个测试全部通过。
- 后端测试命令：`backend\.venv\Scripts\python.exe -m unittest discover backend\tests`
- 后端测试结果：3 个测试全部通过。
- 构建命令：`fnm.exe exec --using v24.16.0 pnpm.cmd run build`
- 构建结果：通过，1740 modules transformed。
- 后端语法检查：`backend\.venv\Scripts\python.exe -m py_compile backend\routers\risk.py backend\routers\hedge.py backend\models\schemas.py`
- 语法检查结果：通过。

## 静态验收

- `rg -n "[\p{Han}]" frontend/src/components frontend/src/App.jsx`：无命中，组件与 App 不再散落中文文案。
- `rg -n "ç|æ|Ã|�|Â|鍔|椤|璁|鐩|洰|鍒|妫|鐢|杩|璐|浣|俙" frontend/src backend/routers`：无命中。

## Browser 可视 QA

- 结果：未完成。
- 原因：Browser 插件三次连接失败，错误为 `windows sandbox failed: spawn setup refresh`。
- 风险评估：这是工具连接风险，不是当前代码测试失败；但该阶段属于可见文案修复，最终合并前仍建议在 `http://127.0.0.1:5173/` 人工确认：
  - 首屏 `HedgeAI`、副标题、聊天标题正常显示。
  - 设置面板文案正常显示。
  - 风险卡片、策略卡片、fallback 标签无乱码。
  - demo/mock 执行结果显示“模拟执行完成”，不显示“交易已提交”。

## 当前判定

- 自动化验收：Go。
- Browser 工具验收：Blocked by tool。
- 监督者复审：Go。
- 阶段结论：Go，允许进入下一阶段；合并或正式演示前建议补一次可用浏览器环境下的视觉 QA。
