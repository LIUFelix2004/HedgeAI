# Supervisor Review: P2-3 测试体系补齐

## Scope

- Branch: `feat/d0-tdd-collaboration-infra`
- Task: P2-3 测试体系补齐
- Reviewer: Lovelace
- Date: 2026-05-29

## TDD Evidence

- Red command: `backend\.venv\Scripts\python.exe -m pytest -q backend\tests`
- Red failure summary: pytest 自动加载本地第三方插件，`web3` 导入 `pkg_resources` 失败，导致测试命令无法启动。
- Red test command: `backend\.venv\Scripts\python.exe -m unittest backend.tests.test_quality_gate`
- Red failure summary: `scripts.quality_gate` 不存在，质量门禁入口无可测配置。
- Green test command: `backend\.venv\Scripts\python.exe -m unittest backend.tests.test_quality_gate`
- Green pass summary: 2 tests passed，覆盖后端 pytest 环境变量和命令构造。
- Green command: `backend\.venv\Scripts\python.exe scripts\quality_gate.py --backend-only`
- Green pass summary: 34 backend tests passed，pytest 自动禁用插件 autoload。
- Green command: `backend\.venv\Scripts\python.exe scripts\quality_gate.py`
- Green pass summary: backend pytest 34 passed，frontend vitest 30 passed，frontend build passed。

## Safety Review

- [x] Demo, dry-run, and real execution safety tests are included in the quality gate.
- [x] Session expiry and credential cleanup tests are included.
- [x] Injective market whitelist and order preview tests are included.
- [x] External APIs are mocked in execution tests.

## UX Review

- [x] Frontend user-visible Demo and execution flows are covered by vitest.
- [x] Mojibake scan is covered by frontend tests.
- [ ] Browser visual QA remains blocked by tool error: `windows sandbox failed: spawn setup refresh`.

## Test Review

- [x] One command runs backend pytest, frontend vitest, and frontend build.
- [x] Backend pytest no longer depends on the caller remembering `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1`.
- [x] README documents the quality gate.
- [ ] CI is not yet configured.
- [ ] Coverage thresholds are not yet configured.

## Verification

- `backend\.venv\Scripts\python.exe scripts\quality_gate.py --backend-only`: 34 tests passed.
- `backend\.venv\Scripts\python.exe scripts\quality_gate.py`: backend 34 passed, frontend 30 passed, build passed.
- Mojibake scan over source paths: no matches in prior P2-1/P1-2 verification; frontend `noMojibake` test is included in the gate.

## Decision

- Status: Go
- Required fixes: None.
- Follow-up risks:
  - 需要 CI 才能保证 PR/MR 自动执行。
  - 需要覆盖率阈值来防止后续回退。
  - Browser 视觉 QA 工具恢复后应纳入门禁。
  - `--backend-only` 与 `--frontend-only` 后续可改为 argparse 互斥参数。
  - 前端门禁依赖本机 `fnm` 和固定 Node `v24.16.0`，CI 中需要显式安装。
