# Supervisor Review: P2-4 统一日志与错误码

## Scope

- Branch: `feat/d0-tdd-collaboration-infra`
- Task: P2-4 统一日志与错误码
- Reviewer: Lovelace
- Date: 2026-05-29

## TDD Evidence

- Red test command: `backend\.venv\Scripts\python.exe -m unittest backend.tests.test_execution_audit`
- Red failure summary: `services.audit_service` 不存在，执行响应缺少 `error_code` 与 `audit_id`。
- Green test command: `backend\.venv\Scripts\python.exe -m unittest backend.tests.test_execution_audit`
- Green pass summary: 4 tests passed，覆盖 blocked execution 的错误码/审计 ID、成功 preview 的审计摘要、核心错误码分类和敏感字段 redaction。
- Green gate command: `backend\.venv\Scripts\python.exe scripts\quality_gate.py`
- First supervisor pass: No-Go。duplicate 错误文本同时包含 `duplicate` 与 `idempotency`，分类顺序导致误归类为 `EXEC_IDEMPOTENCY_REQUIRED`；已补测试并修复为 `EXEC_DUPLICATE_REQUEST`。
- Green gate summary: backend pytest 38 passed with 6 subtests，frontend vitest 30 passed，frontend build passed。
- Refactor notes: 新增 `audit_service` 作为内存审计层；`ExecuteResult` 增加 `audit_id` 和 `error_code`；`hedge.execute_hedge` 统一 finalize 执行结果并记录审计事件。

## Safety Review

- [x] Audit events redact common secret fields.
- [x] Redaction covers `privateKey`、`apiKey`、`apiSecret` and `token`.
- [x] Blocked real execution returns business error code instead of stack traces.
- [x] Success and blocked execution paths both record audit IDs.
- [x] Existing real execution confirmation, idempotency, and risk-limit tests remain in the quality gate.

## Error-Code Review

- [x] Confirmation missing: `EXEC_CONFIRMATION_REQUIRED`.
- [x] Missing or duplicate idempotency: `EXEC_IDEMPOTENCY_REQUIRED` / `EXEC_DUPLICATE_REQUEST`.
- [x] Risk limit: `RISK_LIMIT_EXCEEDED`.
- [x] Unsupported market: `MARKET_UNSUPPORTED`.
- [x] Credential missing: `CREDENTIAL_REQUIRED`.
- [x] Position missing: `POSITION_NOT_FOUND`.
- [x] Unsupported venue: `EXEC_UNSUPPORTED_VENUE`.
- [x] Fallback: `EXECUTION_FAILED`.

## Test Review

- [x] Backend tests cover audit ID and error code contract.
- [x] Full quality gate passes.
- [x] Frontend remains compatible with additional response fields.
- [ ] Persistent audit storage is not covered because it is not implemented in this phase.

## Verification

- `backend\.venv\Scripts\python.exe scripts\quality_gate.py`: backend 38 passed with 6 subtests, frontend 30 passed, build passed.
- Browser visual QA remains blocked by tool error: `windows sandbox failed: spawn setup refresh`.

## Decision

- Status: Go
- Required fixes: None.
- Follow-up risks:
  - Audit events are still in-memory and disappear on process restart.
  - Other routers still need a shared HTTP error envelope.
  - Model/account/market service errors should later map into the same error-code family.
  - 错误码分类仍基于字符串匹配，后续更适合收敛为 typed exception。
