# Supervisor Review: P3-2 模型路由与成本控制

## Scope

- Branch: `feat/d0-tdd-collaboration-infra`
- Task: P3-2 模型路由与成本控制
- Reviewer: Lovelace
- Date: 2026-05-29

## TDD Evidence

- Red test command: `backend\.venv\Scripts\python.exe -m unittest backend.tests.test_model_usage`
- Red failure summary: `services.model_usage_service` 不存在，聊天接口不会记录模型用量，也没有查询接口。
- Green test command: `backend\.venv\Scripts\python.exe -m unittest backend.tests.test_model_usage`
- Green pass summary: 4 tests passed，覆盖 chat message 记录、chat stream 记录、`/api/chat/model-usage` 查询和 `model_api_key` 不泄露。
- Green gate command: `backend\.venv\Scripts\python.exe scripts\quality_gate.py`
- Green gate summary: backend 43 passed with 6 subtests，frontend 31 passed，build passed。
- Supervisor pass: Go。根据后续风险建议，已补 stream 路径用量统计测试和 API key 不泄露测试。

## Product Review

- [x] Existing manual model selection remains intact.
- [x] Current model is already visible in the frontend chat header/message metadata.
- [x] Backend records calls, success, failure, estimated tokens, and estimated cost by model.
- [x] Model usage can be queried through `/api/chat/model-usage`.
- [x] Stream and non-stream chat paths are both covered by tests.
- [x] Tests verify model API keys are not exposed by usage summaries.
- [ ] Automatic fallback is not implemented.
- [ ] Health checks are not implemented.
- [ ] Cost is estimated from output length, not provider usage fields.

## Safety Review

- [x] API keys are not recorded by model usage stats.
- [x] Usage stats are aggregate counters only.
- [x] Existing chat smoke tests remain in the quality gate.

## Verification

- `backend\.venv\Scripts\python.exe scripts\quality_gate.py`: backend 43 passed with 6 subtests，frontend 31 passed，build passed。
- Browser visual QA remains blocked by tool error: `windows sandbox failed: spawn setup refresh`.

## Decision

- Status: Go
- Required fixes: None.
- Follow-up risks:
  - Usage stats are in-memory and reset on restart.
  - Estimated token/cost calculation is rough.
  - No automatic fallback or model health policy yet.
