# Supervisor Review: P1-2 会话与凭证安全

## Scope

- Branch: `feat/d0-tdd-collaboration-infra`
- Task: P1-2 会话与凭证安全
- Reviewer: Lovelace
- Date: 2026-05-29

## TDD Evidence

- Red test command: `backend\.venv\Scripts\python.exe -m unittest backend.tests.test_session_security`
- Red failure summary: 后端缺少断开连接接口，连接结果不包含过期时间，过期会话仍可读取仓位。
- Red test command: `fnm exec --using v24.16.0 pnpm.cmd run test -- SettingsPanel.disconnect.test.jsx`
- Red failure summary: 设置面板中找不到名为“断开连接”的按钮。
- Green test command: `backend\.venv\Scripts\python.exe -m unittest backend.tests.test_session_security`
- Green pass summary: 6 tests passed，会话断开、单账户仓位过期、聚合仓位过期、风险扫描过期清理、执行凭证过期清理和连接过期时间均被覆盖。
- Green test command: `fnm exec --using v24.16.0 pnpm.cmd run test -- SettingsPanel.disconnect.test.jsx`
- Green pass summary: 16 files / 29 tests passed，新增断开连接测试通过。
- Refactor notes: 本阶段保持内存会话结构，仅补齐 TTL、过期清理、主动断开和前端本地敏感状态清理；持久化 session service 和审计日志留给后续阶段。
- Supervisor first pass: No-Go。risk 和 hedge 存在直接读取 `_sessions` 的绕过路径，已补 Red 测试并修复为 active-session 读取。

## Safety Review

- [x] Demo, dry-run, and real modes are visually and behaviorally distinct.
- [x] Real execution is blocked on the backend when confirmation or credentials are missing.
- [x] Dry-run output cannot be mistaken for a submitted trade.
- [x] Fallback strategy output is labeled as local fallback, not AI output.
- [x] No UI copy claims unsupported live execution.
- [x] Active disconnect removes backend session credentials.
- [x] Expired sessions are removed before positions are returned.
- [x] Expired sessions are removed before aggregate positions are returned.
- [x] Expired sessions are not used by risk scan.
- [x] Expired trading credentials are not used by real execution.
- [x] Frontend disconnect clears local account fields, secret fields, and positions.

## UX Review

- [x] No visible mojibake or broken copy in touched UI strings.
- [x] Buttons and labels fit on desktop and mobile by existing panel constraints.
- [x] anime.js motion is not part of this task.
- [x] Error messages include a clear next action.
- [ ] Browser visual QA is blocked by tool error: `windows sandbox failed: spawn setup refresh`.

## Test Review

- [x] Frontend tests cover user-visible behavior.
- [x] Backend tests cover API contracts and safety boundaries.
- [x] External APIs are mocked in tests.
- [x] Build passes.

## Verification

- `backend\.venv\Scripts\python.exe -m unittest discover backend\tests`: 28 tests passed.
- `fnm exec --using v24.16.0 pnpm.cmd run test`: 16 files / 29 tests passed.
- `fnm exec --using v24.16.0 pnpm.cmd run build`: passed.
- Mojibake scan over touched source paths: no matches.

## Decision

- Status: Go after supervisor No-Go fixes
- Required fixes: None before review.
- Follow-up risks:
  - 内存 `_sessions` 仍不是生产级会话存储，后续需要抽象 session service 并接入 SQLite/Redis。
  - 后端凭证仍在进程内短期保存，后续需要凭证保险箱或加密存储方案。
  - 审计日志尚未持久化，需在 P2-4 统一日志与错误码中补齐。
